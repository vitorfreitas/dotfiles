# dotfiles

always WIP.

## nvim setup

1. `:PlugInstall`
2. install the following coc extensions (`:CocInstall`):
    * coc-tsserver
    * coc-solargraph
    * coc-json
    * coc-eslint

## tmux setup

`tmux source-file ~/tmux/.tmux.conf`
